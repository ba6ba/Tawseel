<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.Toolbar
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="@dimen/x80"
        android:background="@drawable/top"
        android:id="@+id/toolbar"
        app:collapseIcon="@drawable/back"
        app:titleMarginBottom="@dimen/x40"
        app:navigationIcon="@drawable/ic_navigation_white_24dp"
        app:title="@string/business_description"
        app:titleTextAppearance="@style/Tawseel.TextView.Medium.White"
        app:layout_constraintStart_toStartOf="parent" app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>